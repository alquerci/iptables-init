iptables-init
=============

An useful bash script for auto-restore iptables rules.

REQUIREMENT
-----------

* Basic unix utils
* git
* make

INSTALL
-------

$ `make`
$ `sudo PWD=$(pwd) make install`

